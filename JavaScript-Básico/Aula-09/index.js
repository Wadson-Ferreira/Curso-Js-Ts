/* var nome = 'Luiz';
var nome = ' Otávio';
const nome = 'otávio';

let nome = 'Luiz' //Causa erro pois estamos redeclarando uma variável e let não permite esse comportamento.
console.log(nome);
*/

const nome = 'Luiz'; //String
const nome1 = "Luiz"; //String
const nome2 = `Luiz`; //String

const num1 = 10; //Number
const num2 = 10.52; //Number
let nomeAluno; // undefined -> Não aponta para local nenhum na memória
const sobrenomeAluno = null; // Nulo -> não aponta para nenhum local na memória
const boolean = true; // Tem somente dois valores possíveis, true e false. (é um valor lógico)

console.log(typeof nome, nome);